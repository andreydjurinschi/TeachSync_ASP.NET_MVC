@using System.Security.Claims
@model Replacement

<div class="container mt-4">
    <h2 class="mb-4">Approve Replacement</h2>

    <form asp-action="Approve" method="post" class="card p-4 shadow-sm">
        <input type="hidden" name="replacementId" value="@Model.Id" />
        <input type="hidden" name="teacherId" value="@User.FindFirstValue(ClaimTypes.NameIdentifier)" />

        <p>Вы собираетесь подтвердить замену.</p>

        <div class="d-flex justify-content-end">
            <a asp-action="Index" class="btn btn-secondary me-2">Cancel</a>
            <button type="submit" class="btn btn-primary">Approve</button>
        </div>
    </form>
</div>
